# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Package managers
node_modules
bun.lockb
dist
dist-ssr
dev-dist
*.local

# Supabase local dev artifacts
supabase/.temp/
supabase/.branches/

# VAPID keys (Web Push) – não commitar
vapid.json
vapid_public.txt

# Saída do crawler RAG (upload no GCS; não commitar)
crawl-output/

# FCM / Firebase service account key (upload no EAS; não commitar)
*-firebase-adminsdk-*.json
*service-account*.json

# Firebase Android client (google-services.json; não commitar)
mobile/google-services.json
mobile/google-services*.json

# Variáveis de ambiente — nunca commitar segredos (modelo: .env.example, .env.e2e.example)
.env
.env.*
!.env.example
!.env.e2e.example

# GeoSampa sync – config com URLs (use .example como modelo)
scripts/geosampa-layers.json


# Test coverage
coverage/
playwright-report/
test-results/

# Editor directories and files
.cursor/
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Office temp files (Word/Excel)
~$*.docx
~$*.xlsx
~WRL*.tmp

# Expo config files (only mobile/ should have these)
app.json
eas.json