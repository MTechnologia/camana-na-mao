/**
 * Google Maps JavaScript API é carregada via script; tipagem mínima.
 * Para tipos completos: npm i -D @types/google.maps
 */
declare global {
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  const google: any;
}

export {};
