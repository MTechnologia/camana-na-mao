/**
 * @deprecated Barra unificada incorporada em `AdminHeader` (`showAnalyticsFilters`).
 * Mantido para compatibilidade de importações legadas.
 */
export { AdminHeader as UnifiedAnalyticsContextBar } from './AdminHeader';
